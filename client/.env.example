# Dev: leave empty — Vite proxy forwards /api → localhost:3000
# Prod: set to your deployed server URL e.g. https://helpin-server.onrender.com
VITE_API_URL=

# Must match API_KEY in server/.env (bundled into client — ok since it’s an access token, not a secret)
VITE_API_KEY=your_64_char_hex_api_key
